.. highlight:: shell

============
Installation
============


Stable release
--------------

To install igit, run this command in your terminal:

.. code-block:: console

    $ pip install igit

This is the preferred method to install igit, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From source
-----------

The source for igit can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/jmosbacher/igit

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/jmosbacher/igit/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ pip install .

.. _Github repo: https://github.com/jmosbacher/igit
.. _tarball: https://github.com/jmosbacher/igit/tarball/master
